# spam-email-detection-ml-model
This project implements a predictive machine learning model for spam email classification. It includes data preprocessing, text feature extraction using TF-IDF, model training with Multinomial Naive Bayes, and evaluation using accuracy, precision, recall, F1-score, and confusion matrix.
